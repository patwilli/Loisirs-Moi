import React from 'react';
import Header from '../components/Header';

const Home = () => {
    return (
        <div>
            <Header />
            <main>
                Accueil
            </main>
        </div>
    );
};

export default Home;
